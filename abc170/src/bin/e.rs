#![allow(non_snake_case)]
#![allow(unused_variables)]

use competitive_hpp::prelude::*;

#[fastout]
fn main() {
    input! {
        N: usize, Q: usize,
        AB: [(i64, Usize1); Q],
        CD: [(Usize1, Usize1); Q]
    }

    println!("{}", a)
}
